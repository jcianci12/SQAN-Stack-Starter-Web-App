import { StarttimeisbeforeendtimePipe } from './starttimeisbeforeendtime.pipe';

describe('StarttimeisbeforeendtimePipe', () => {
  it('create an instance', () => {
    const pipe = new StarttimeisbeforeendtimePipe();
    expect(pipe).toBeTruthy();
  });
});
