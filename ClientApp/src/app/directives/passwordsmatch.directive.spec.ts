import { PasswordsmatchDirective } from './passwordsmatch.directive';

describe('PasswordsmatchDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordsmatchDirective();
    expect(directive).toBeTruthy();
  });
});
