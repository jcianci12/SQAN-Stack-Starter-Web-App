import { GetRowColumnPipe } from './get-row-column.pipe';

describe('GetRowColumnPipe', () => {
  it('create an instance', () => {
    const pipe = new GetRowColumnPipe();
    expect(pipe).toBeTruthy();
  });
});
